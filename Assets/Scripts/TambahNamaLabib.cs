using UnityEngine;

public class TambahNamaLabib : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama: M.Labib Abrar");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
